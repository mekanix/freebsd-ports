#!/bin/sh

. /etc/rc.subr

name="jbod"
start_cmd="${name}_start"
stop_cmd="${name}_stop"

jbod_start() {
	touch "/var/run/${name}"
}

jbod_stop() {
	rm "/var/run/${name}"
}

load_rc_config $name
run_rc_command "$1"
