# $FreeBSD$

PORTVERSION=	1.0.2
PORTREVISION=	2
PKGNAMESUFFIX=	-stg

GH_TAGNAME=	${PORTVERSION}

CONFLICTS_INSTALL=	dcpkg

MASTERDIR=	${.CURDIR}/../dcpkg
DISTINFO_FILE=	${.CURDIR}/distinfo

.include "${MASTERDIR}/Makefile"
